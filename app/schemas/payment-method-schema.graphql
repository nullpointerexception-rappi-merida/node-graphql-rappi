type PaymentMethod {
    _id: ID!
    cardNumber: String!
    cardHolder: String!
    expires: String!
    isActive: Boolean
}

input createPaymentMethodInput {
    cardNumber: String!
    cardHolder: String!
    expires: String!
}

input updatePaymentMethodInput {
    _id: ID!
    cardNumber: String!
    cardHolder: String!
    expires: String!
}
