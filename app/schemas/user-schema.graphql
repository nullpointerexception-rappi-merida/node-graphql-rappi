scalar Upload
type Auth {
    token: String!
    message: String!
    hasProfile: Boolean
}

type User {
    _id: ID
    email: String!
    phone: String
    password: String!
    isActive: Boolean
}

type Profile {
    _id: ID!
    firstName: String!
    lastName: String!
    birthDate: String!
    gender: String!
    profilePicture: Upload
}

type UserAndProfile {
    _id: ID
    email: String!
    phone: String
    userProfile: Profile
}

input createUserInput {
    email: String!
    phone: String!
    password: String!
}

input createProfileInput {
    firstName: String!
    lastName: String!
    birthDate: String!
    gender: String!
    profilePicture: Upload
}

input updateProfileInput {
    firstName: String
    lastName: String
    birthDate: String
    gender: String
    profilePicture: Upload
}
